import React,{useState} from 'react';
export default function CreatePostInline({onCreate}){const [title,setTitle]=useState(''),[content,setContent]=useState('');return <div className="soft-card p-5"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Título do post" className="w-full bg-transparent text-lg font-bold outline-none"/><textarea value={content} onChange={e=>setContent(e.target.value)} rows="3" placeholder="Compartilhe algo com a comunidade..." className="mt-3 w-full resize-none rounded-xl border border-zinc-700 bg-zinc-950 p-3"/><button onClick={()=>{if(title&&content){onCreate({title,content});setTitle('');setContent('')}}} className="mt-3 rounded-xl bg-gradient-to-r from-violet-600 to-fuchsia-600 px-4 py-2 font-bold">Publicar</button></div>}
