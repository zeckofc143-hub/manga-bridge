import React from 'react'; import {Heart} from 'lucide-react'; import {formatDate} from '@/lib/utils';
export default function PostCard({p,onLike}){return <article className="soft-card p-5"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-gradient-to-br from-violet-600 to-fuchsia-600 font-bold">{p.author_name?.[0]}</div><div><b>{p.author_name}</b><div className="text-xs text-zinc-600">{formatDate(p.created_date)}</div></div></div><h3 className="mt-4 font-display text-lg font-bold">{p.title}</h3><p className="mt-2 whitespace-pre-wrap text-sm text-zinc-400">{p.content}</p><button onClick={()=>onLike?.(p)} className="mt-4 flex items-center gap-2 text-sm text-zinc-400 hover:text-fuchsia-300"><Heart size={17}/>{p.like_count||0}</button></article>}
