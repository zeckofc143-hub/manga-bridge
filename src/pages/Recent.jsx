import React,{useMemo,useState} from 'react';import {useQuery} from '@tanstack/react-query';import {backend} from '@/api/localBackend';import ManhwaCard from '@/components/manhwa/ManhwaCard';
export default function Recent(){const [type,setType]=useState('all');const {data:items=[]}=useQuery({queryKey:['manhwas'],queryFn:()=>backend.entities.Manhwa.list()});const out=useMemo(()=>[...items].sort((a,b)=>new Date(b.created_date)-new Date(a.created_date)).filter(m=>type==='all'||m.type===type),[items,type]);return <div className="mx-auto max-w-7xl px-4 py-10"><h1 className="font-display text-3xl font-extrabold">Lançamentos recentes</h1><div className="mt-5 flex flex-wrap gap-2">{[['all','Todos'],['manhwa','Manhwa'],['manga','Manga'],['manhua','Manhua'],['novel','Novel']].map(([k,l])=><button onClick={()=>setType(k)} className={`rounded-full px-4 py-2 text-sm ${type===k?'bg-violet-600':'bg-zinc-800'}`} key={k}>{l}</button>)}</div><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6">{out.map(m=><ManhwaCard key={m.id} m={m}/>)}</div></div>}