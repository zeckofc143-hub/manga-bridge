import React from 'react';import {useQuery} from '@tanstack/react-query';import {backend} from '@/api/localBackend';import {useAuth} from '@/lib/AuthContext';import ManhwaCard from '@/components/manhwa/ManhwaCard';
export default function Favorites(){const {user}=useAuth();const {data:lib=[]}=useQuery({queryKey:['library',user?.email],queryFn:()=>backend.entities.UserLibrary.filter({user_email:user.email}),enabled:!!user});const {data:m=[]}=useQuery({queryKey:['manhwas'],queryFn:()=>backend.entities.Manhwa.list()});const ids=new Set(lib.filter(x=>x.is_favorite).map(x=>x.manhwa_id));return <div className="mx-auto max-w-7xl px-4 py-10"><h1 className="font-display text-3xl font-extrabold">Favoritos</h1><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6">{m.filter(x=>ids.has(x.id)).map(x=><ManhwaCard key={x.id} m={x}/>)}</div>{!ids.size&&<p className="mt-10 text-zinc-500">Favorite uma obra para ela aparecer aqui.</p>}</div>}