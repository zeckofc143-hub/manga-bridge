import React from 'react';import {useQuery} from '@tanstack/react-query';import {backend} from '@/api/localBackend';import ManhwaCard from '@/components/manhwa/ManhwaCard';
export default function Popular(){const {data:items=[]}=useQuery({queryKey:['manhwas'],queryFn:()=>backend.entities.Manhwa.list()});const out=[...items].sort((a,b)=>(b.views||0)-(a.views||0));return <div className="mx-auto max-w-7xl px-4 py-10"><h1 className="font-display text-3xl font-extrabold">Mais vistos</h1><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6">{out.map(m=><ManhwaCard key={m.id} m={m}/>)}</div></div>}